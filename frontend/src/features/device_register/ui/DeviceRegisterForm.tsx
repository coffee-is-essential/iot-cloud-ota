import { JSX } from "react";

export interface DeviceRegisterFormProps {
  onClose?: () => void;
}

export const DeviceRegisterForm = ({
  onClose,
}: DeviceRegisterFormProps): JSX.Element => {
  return <div>DeviceRegisterForm</div>;
};
